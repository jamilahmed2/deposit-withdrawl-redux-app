import React from "react";

const Balance = () => {
  return (
    <div>
      <button type="button" disabled className="btnd">
        Your Balance $0
      </button>
    </div>
  );
};

export default Balance;
